# --- Ignore entire folders you don’t need in the VSIX ---
archive/
backup/
node_modules/
.vscode/

# --- Ignore project-config and build-only files ---
.vscode-test.mjs
.vscodeignore
.gitignore
vsc-extension-quickstart.md
eslint.config.mjs
tsconfig.json
package-lock.json
*.tgz

# --- Ignore any existing VSIX artifacts ---
*.vsix

# --- Exclude all your TypeScript source… ---
src/

# --- Ensure compiled output is included ---
!out/

# Ensure compiled files are included
!out/**/*.js
!out/**/*.map